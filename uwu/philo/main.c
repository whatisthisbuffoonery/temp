#include "thing"

void	print_thing(t_philo *philo)
{
	static //
	//will get its own timestamp

int	args(int c, char **v)
{
	if (c != 5 && c != 6)
		return (1);
}

//if time to eat == time to die, they will die immediately
int	main(int c, char **v)
{
	if (args(c, v))
		return (1);
