root list

AppArmor needs to run at startup

need 2 LVM encrypted partitions

difference between 'aptitude' and apt (what, english?)

"an ssh thing sits on port 4242. So root cannot ssh in at all"

UFW (firewall)

hostname login42 
^^^user needs to be in user42 and sudo groups



password must expire every 30 days
min num of days before password change is 2 days
warn for password expire 7 days before
password: 10 chars, upper/lowercase, number, no 3 same letters, no the name of user
user password should have 7 non appearing chars from the root password



--
pdf says need to change passwords after writing config files
--

sudo auth 3 attempts
custom err msg on wrong sudo password
log sudo commands AND outputs in /var/log/sudo/ folder
tty mode must be enabled
paths that can be used by sudo to be restricted:
/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/snap/bin

make monitoring.sh:
	server startup and every 10 mins: info
		architecture
		cpu physical
		vcpu
		mem usage
		disk usage
		cpu load

^^^need to use cron to interrupt this script during eval

(need to modify hostname during eval)
need to make new user and assign to group during eval
